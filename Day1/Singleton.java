package review.flab.vincent;

public interface Singleton {
    //private Singleton();
    //두 싱글톤 패턴의 구현체를 interface 로 묶어보려고 시도하다가 실패. static getInstance 는 interface 에서 선언한 뒤 오버라이드 시키는 식으로 구현할 수 없었다.
    //이런 경우에는 어떻게 묶어줘야 하는 걸까?
}
